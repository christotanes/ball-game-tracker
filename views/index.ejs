<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NBA Tracker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="./styles.css">
</head>

<body>

    <nav class="navbar navbar-expand-lg bg-body-primary">
        <div class="container-fluid">
          <a class="navbar-brand" href="#">NBA Games</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon bs-secondary-color"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">

                <!-- A for of loop that will create a number of links corresponding to how many games were/are played that day -->
            <% for(let match of games){ %>
                <a href="/<%= match.cardData.awayTeam.teamTricode.toLowerCase() %>-vs-<%= match.cardData.homeTeam.teamTricode.toLowerCase()%>-<%= match.cardData.gameId %>" class="nav-link">  <%= match.cardData.awayTeam.teamTricode%> vs <%= match.cardData.homeTeam.teamTricode%></a>
            <%}%>
            </div>
          </div>
        </div>
      </nav>

    <section>
        <div id="headlines">

            <!-- A for of loop that will create a number of divs containing links filled with headlines of the day -->
            <% for(let headline of headlines){ %>
                <div class="headlineTitle">  
                    <a href="<%= headline.permalink %>" target="blank" class="headlineLink"><%= headline.title %></a>
                </div>
            <% }%>

            <!-- Span with css that will animate how much time left before headline changes/slides -->
            <span class="timer"></span>
        </div>
    </section>
    
    <% if(locals.games) { %>
        <%- include("matches.ejs") %>   
    <% }else if(locals.boxscore){ %>
        <%- include("boxscore.ejs") %>
    <% } %>

    <%- include("footer.ejs") %>

</body>
</html>